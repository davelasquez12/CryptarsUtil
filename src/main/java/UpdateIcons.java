import java.util.Map;

public class UpdateIcons {
    public static final String test = "test";
    public void execute(Map<String, String> iconUrlMapData) {
        //get list of supported assets from CurrentCryptoPrices table in DynamoDB

        //compare list with the iconUrlsMapData to record the asset icons that are supported

        //store recorded urls to the CurrentCryptoPrices table in a new column "assetIcon"
    }
}
